# Calendar Domain

## Scope

Calendar exposes API endpoints for events and deadline synchronization tied to user tasks.

## Highlights

- Events are user-scoped
- Validation ensures end_time is after start_time
- Deadline sync endpoints manage auto-sync settings

## Key Endpoints

- /api/v1/calendar/events/
- /api/v1/calendar/deadline-sync/
